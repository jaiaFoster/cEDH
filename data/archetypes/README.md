# Archetype registry

Empty. Blocked on TopDeck.gg/EDHTop16 access (`coverage_backlog/BACKLOG.md`
`ENV-0001`, `INFRA-0001`). Built from tournament data first per charter, not
a hand-picked commander list. One subdirectory per
`archetypes-<tournament-window>` registry version; files conform to
`data/schemas/archetype.schema.json`.
