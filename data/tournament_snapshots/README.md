# Tournament data snapshots

Empty. Blocked on TopDeck.gg/EDHTop16 access (`coverage_backlog/BACKLOG.md`
`ENV-0001`). Once reachable, pulls land here as
`<topdeck+edhtop16-window>/` directories with raw source JSON plus a
`pull_manifest.json` recording pull date, source API version/response
headers if available, and the exact query parameters used — so a pull is
re-runnable and its provenance is legible without re-deriving it from the
archetype registry that consumed it.
