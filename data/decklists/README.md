# Decklists

Empty. Waiting on the subject decklist (see `coverage_backlog/BACKLOG.md`
`DECK-0001`). Each file here is one immutable versioned decklist conforming
to `data/schemas/decklist.schema.json` — see `docs/VERSIONING.md` for the
`<deckname>-v<N>` naming convention and the immutability rule (a changed list
is a new file, not an edit).
