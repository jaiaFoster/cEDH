# Coverage backlog

See `README.md` in this directory for the lifecycle and ranking method. This
file must stay in sync with `backlog.jsonl`.

## Open

| ID | Area | Summary | Impact | Opened |
|---|---|---|---|---|
| SIM-0001 | RULES | SIM-001's subject decklist (98 cards + 2 commanders) is transcribed from the assignment text, not independently re-pulled from the supplied Moxfield URL. Root cause changed: Moxfield itself (Cloudflare bot-wall, `Please enable cookies`) now blocks us, not our proxy — confirmed `moxfield.com` and `api.moxfield.com` both reachable at the TCP/proxy level but 403 from Moxfield's own bot protection. | If the assignment's transcription has any error, or the live Moxfield list has since changed, the deck version `tymna-thrasios-treefarm-v1` is wrong. Partially mitigated: every card's own identity/legality is now independently confirmed via the Scryfall bulk pull, so the residual risk is narrowed to "wrong card in the list" / "wrong quantity", not "wrong card data." | 2026-08-12 |
| SIM-0004 | SIM | Volatile Stormdrake uses the Energy ({E}) counter mechanic; no other card in the subject 98 appears to produce or spend Energy (confirmed via full bulk ingestion, not just spot-check). Forge's card script for it (`v/volatile_stormdrake.txt`) does correctly implement Energy natively (`CounterType$ ENERGY`), so an eventual Forge-backed adapter gets this for free — a native tracker would still need to build it. | The rules-engine state tracker (`sim/rules_engine/`) needs explicit support for Energy as a resource type for this one card, or its ETB ability can't be modeled correctly, if we go the native-tracker route. | 2026-08-12 |
| SIM-0012 | INTERACT | `INT-0011`'s original candidate writeup claimed Derevi's combat-damage untap trigger "scales with attacker count" (N connecting creatures → N independent untap-then-retap cycles on Gaea's Cradle). Only the single-attacker base case was engine-verified; a two-attacker extension test failed and was not resolved within that interaction's effort budget. | The multi-attacker scaling claim is downgraded to "plausible per the underlying rules mechanism (CR 603.2c, 603.3b, 117.3b), not itself engine-verified" in `interactions/verified/INT-0011.json` — must not be treated as confirmed until a working multi-attacker test exists. | 2026-08-12 |

## Ranking (frequency × impact, per README.md's method)

As of 2026-08-12, ranked by what actually blocks the most downstream work,
not by ID order:

1. **`SIM-0001` — moderate impact, low frequency, but foundational.** Every
   downstream deck-backed artifact (Gate 1 closure, gold games, eventual
   simulation) inherits whatever risk this carries. Frequency is low (one
   check, not recurring) but the *consequence* of being wrong is systemic
   — the entire frozen deck hash, every card record, and every future
   simulation config all key off `tymna-thrasios-treefarm-v1`. Ranked above
   `SIM-0004` despite lower urgency because a wrong subject deck silently
   invalidates everything else, whereas a missing Energy mechanic only
   affects one card's fidelity.
2. **`SIM-0012` — low-moderate impact, low frequency.** A residual, narrowly-
   scoped gap (whether Derevi+Cradle's untap-then-retap mechanism scales
   with attacker count) inside an already-verified interaction; the base
   case is trustworthy, only the extrapolation is unconfirmed. Cheap to
   close once revisited (needs a working two-attacker XMage test), but not
   urgent since nothing currently depends on the multi-attacker case being
   proven.
3. **`SIM-0004` — low impact, lowest frequency.** Affects exactly one
   card's ETB ability (Volatile Stormdrake) and only matters if/when a
   native (non-Forge) rules tracker is built — moot entirely if the
   eventual `sim/rules_engine/` adapter wraps XMage or Forge directly
   (both already implement Energy natively, confirmed for Forge). Lowest
   priority of the open items; revisit only once the native-vs-
   engine-wrapper architecture decision for `sim/rules_engine/` is made.

## Resolved

| ID | Area | Summary | Resolution | Resolved |
|---|---|---|---|---|
| DECK-0001 | RULES | No subject decklist had been supplied to this repository. | SIM-001 supplied the exact subject decklist (Tymna the Weaver / Thrasios, Triton Hero, 98 main-deck cards). Transcribed into `data/decklists/_provisional/tymna-thrasios-treefarm-v1.json`. | 2026-08-12 |
| ENV-0001 | ENV | This execution environment's outbound network egress was blocked (403 at the proxy) to api.scryfall.com, mtgjson.com, backend.commanderspellbook.com, topdeck.gg, edhtop16.com, github.com, and moxfield.com. | User updated the environment's network egress policy (a different settings surface than the general claude.ai capability toggle — see `docs/EGRESS_ALLOWLIST.md`). Re-tested with real content pulls: Scryfall, MTGJSON, Commander Spellbook, TopDeck.gg, EDHTop16, and GitHub all return live data. Moxfield remains blocked, but by Moxfield's own Cloudflare bot-wall, not this proxy — tracked separately as part of SIM-0001. | 2026-08-12 |
| SIM-0002 | RULES | 75 of the 98 subject-deck cards were marked `well_known` (asserted from prior knowledge, not verified). | Superseded: all 100 cards (98 + 2 commanders) bulk-ingested from Scryfall's `/cards/collection` endpoint with full Oracle text, mana cost, color identity, and rulings, via `sim/ingestion/scryfall.py`. Every card is now authoritative-source-verified, not memory- or search-derived. Found 5 further corrections in the process — see SIM-0006. | 2026-08-12 |
| SIM-0003 | RULES | Commandeer's exact printed mana cost was not confirmed (only the pitch alt-cost was). | Confirmed via bulk ingestion: `{5}{U}{U}`. Alt cost also reconfirmed: exile two blue cards from hand. | 2026-08-12 |
| SIM-0005 | INTERACT | Several interaction lines named in the SIM-001 assignment's discovery seed list (Oboro Breezecaller lines, Shifting Woodland, Devoted Druid lines, Faerie Mastermind + Smothering Tithe loops) had not been checked against Commander Spellbook. | Built `sim/ingestion/spellbook.py`, queried the `/variants` endpoint once per subject-deck card (3,853 unique variants touched this deck), and classified each by card-set overlap. Found 6 variants fully playable with only cards already in the deck (written to `interactions/candidate/`) — directly confirming 3 of the assignment's named seeds: Devoted Druid + Swift Reconfiguration (infinite green mana), Devoted Druid + Hazel's Brewmaster (a second, independent infinite-green-mana line), and Faerie Mastermind + Smothering Tithe (3 variants: + Clever Impersonator, + Kinnan, or + Training Grounds, all infinite card draw/Treasures). Also confirmed Oboro Breezecaller + Talon Gates of Madara + Gaea's Cradle as a real line (infinite mana + landfall + repeatable phase-out). Also logged 112 "one card away" near-misses for later deckbuilding reference (not written as candidate interactions since they aren't real lines in this exact 98). All 6 remain `status: candidate`, not `verified` — see new `SIM-0007` for the Phase 3 validation this still needs. | 2026-08-12 |
| INFRA-0001 | INFRA | EDHTop16's live API base URL was unconfirmed (edhtop16.com vs legacy cedhtop16.com/api). | Confirmed via live probing now that ENV-0001 is resolved: `cedhtop16.com/api` redirects (302) away — it's the legacy host. The current live API is a GraphQL endpoint at `edhtop16.com/api/graphql` (returns 200). Full schema exploration deferred to Phase 6 (metagame census) proper. | 2026-08-12 |
| INFRA-0002 | INFRA | Forge vs. XMage vs. a native Level 1-2 tracker had not been decided; both engines' coverage of the subject deck was unknown. | Shallow-cloned both `Card-Forge/forge` (815MB, `--depth 1 --filter=blob:none`) and `magefree/mage` (164MB, sparse-checkout of `Mage.Sets/src/mage/cards`). Checked all 100 subject-deck cards against each engine's card-script/class files by name-slug matching: **100/100 in Forge, 100/100 in XMage** (one apparent XMage miss, King T'Challa, was a bug in this agent's own slug-capitalization logic — `KingTChalla.java` does exist; corrected before reporting). Spot-checked Forge's scripts for Devoted Druid, Swift Reconfiguration, and Volatile Stormdrake — all three faithfully implement the Oracle text, including Volatile Stormdrake's Energy mechanic natively. Both engines are viable; remaining choice (ergonomics/performance, not coverage) tracked as new `INFRA-0003`. Clones were scratch-only, not committed to this repo. | 2026-08-12 |
| INFRA-0003 | INFRA | Remaining choice between Forge and XMage was scripting/API ergonomics and Level 3 performance, not coverage (INFRA-0002 already confirmed 100/100 both). | Did a full (non-sparse) clone of XMage and a scoped Maven build (`Mage`, `Mage.Sets`, `Mage.Tests` modules, JDK 21). **Proven working**: XMage's pre-existing `DevotedDruidTest.java` (using a JUnit DSL - `addCard`, `activateAbility`, `checkPermanentTapped`, `waitStackResolved`, `execute()` - closely matching this project's own gold-board-state shape) compiles and runs 3/3 passing, including a rules-nuanced case involving Vizier of Remedies modifying -1/-1 counter placement. This is real evidence XMage's `Mage.Tests` framework is a viable Level 4 / Gate 2 substrate for this project, not just a plausible one. A follow-on attempt to write a NEW test against one of this project's own candidate interactions (`INT-0002`) did not succeed at first - tracked as `SIM-0008`, since resolved (see below). Forge not build-tested this pass (XMage's existing test-authoring fit made it the natural first candidate to prove out) - could still be revisited. | 2026-08-12 |
| SIM-0008 | INFRA | A new XMage test (`DevotedDruidSwiftReconfigurationTest`, written to validate `INT-0002`) failed with "no such permanent on the battlefield, cardName=Devoted Druid" after `castSpell(..., "Swift Reconfiguration", "Devoted Druid")` + `waitStackResolved`, even after fixing a mana-color bug and adding `setStrictChooseMode(true)`. | Blocked a real Level 4 engine reproduction of `INT-0002`. | Root cause found on the 5th attempt: `assertType`/`assertPermanentCount` (no turn/step args) are IMMEDIATE checks against `currentGame`, but every other call in the test (`addCard`, `castSpell`, `activateAbility`, `waitStackResolved`, `checkXxx`-with-turn-step-args) only REGISTERS a scheduled action/check - the actual game doesn't run until `execute()` is called once at the very end. Calling `assertType` before `execute()` checks a game that hasn't started. Separately (6th attempt): the test never added Swift Reconfiguration to the player's hand at all, so there was nothing to cast. Fixed both; test now passes 1/1. `INT-0002` promoted to `interactions/verified/`. | 2026-08-12 |
| SIM-0006 | RULES | Full Scryfall bulk ingestion (100/100 cards) surfaced 5 further color-identity/mana-cost corrections beyond the 23 WebSearch-spot-checked cards, in cards previously marked `well_known`: Avacyn's Pilgrim (W → W+G, mana ability adds G), Delighted Halfling (guessed W → actually G), Elves of Deep Shadow (B → B+G, mana ability adds G), Gaea's Cradle (guessed colorless → actually G, mana ability), and Swift Reconfiguration (the WebSearch "verification" itself was wrong: said mono-blue {U}, authoritative data says mono-white {W}). | None of the 5 changed the legality verdict (still WUBG, confirmed by a full programmatic union-and-subset check against commander identity — zero violations across all 100 cards). Swift Reconfiguration is a concrete demonstration that WebSearch-level "verification" is not reliable enough to trust over authoritative bulk data — documented in `docs/assignments/SIM-001.md` as a methodology finding. *(Note: this entry was miscategorized under "Open" in an earlier commit despite backlog.jsonl already marking it resolved — moved here as a self-correction; see the strengthened `test_backlog_sync.py` check this bug prompted.)* | 2026-08-12 |
| SIM-0009 | INTERACT | `INT-0013` (Delney, Streetwise Lookout doubling Kinnan, Bonder Prodigy's own mana-doubling trigger — +2 mana per qualifying nonland mana activation) needed Level 4 validation like `INT-0002` before it could be trusted as a deterministic transition. | Downloaded the current official Comprehensive Rules text directly from `media.wizards.com` (the `magic.wizards.com/en/rules` page links the live `.txt`; the previously-known PDF filename pattern had rotated and 404'd, so the link was re-discovered rather than guessed) and confirmed the exact citations: CR 603.2d (an ability may state a triggered ability triggers additional times), CR 605.1b (criteria for a triggered ability to itself be a mana ability — Kinnan's trigger meets all three), and CR 605.4/605.4a (triggered mana abilities follow rule 603 but skip the stack, resolving immediately — explaining why the engine test needed no explicit stack-wait). Reproduced in the same from-source XMage build used for `SIM-0008`/`INT-0002`: a new test, `DelneyKinnanTripleManaTest`, passed 1/1 on the first attempt (no authoring bugs this time). `INT-0013` promoted to `interactions/verified/`; backing gold board state `rules_tests/gold_board_states/GBS-0002.json`. *(Correction, 2026-08-12: the initial writeup wrongly generalized the 1-mana-source result (Devoted Druid, 1 → 3) into "triples all nonland mana sources" — user review caught this. Kinnan's granted trigger adds a flat one mana per instance, not a multiple of the source's own output, so the correct general model is `total = source's own mana + 2`. Confirmed by a second engine test against a 2-mana source, Sol Ring (2 → 4, not 6): `DelneyKinnanSolRingPlusTwoTest`, 1/1 passing, backing new gold board state `rules_tests/gold_board_states/GBS-0003.json`. Both `INT-0013.json` and `GBS-0002.json` corrected to state the flat +2 model explicitly.)* | 2026-08-12 |
| SIM-0010 | INTERACT | `INT-0007` (Delney, Streetwise Lookout doubling Orcish Bowmasters' compound ETB/opponent-draw trigger) needed Level 4 validation like `INT-0002`/`INT-0013` before it could be trusted as a deterministic transition. | Reproduced in the same from-source XMage build: a new test, `DelneyOrcishBowmastersDoubleTest`, casts Orcish Bowmasters (it has Flash) so its "enters the battlefield" trigger genuinely fires (fixture-placed battlefield cards don't trigger ETB in this test framework), producing two independent Delney-doubled trigger instances. First attempt failed with a "Missing CHOICE def" error — strict-choose mode requires an explicit trigger-ordering choice even when both pending triggers are identical; fixed with `setChoice(playerA, "Orcish Bowmasters")`. Passed 1/1 on the second attempt: 2 damage (life 20→18) and Amass Orcs 2 as a single Orc Army Token with 2 +1/+1 counters (not two separate tokens — confirmed against CR 701.47a, which only creates a new Army token if the controller doesn't already have one). Rules citations: CR 603.2d, CR 603.3b (ordering simultaneously-pending triggered abilities), CR 701.47a (amass). `INT-0007` promoted to `interactions/verified/`; backing gold board state `rules_tests/gold_board_states/GBS-0004.json`. | 2026-08-12 |
| SIM-0011 | INTERACT | `INT-0008` (Esper Sentinel), `INT-0009` (Runic Armasaur), `INT-0010` (Spellseeker), and `INT-0014` (Archivist of Oghma) — the remaining 4 Delney-doubling candidates — needed Level 4 validation. | All 4 reproduced 1/1 in the same from-source XMage build, closing the entire Delney-doubling sub-cluster (`INT-0007..0010`, `INT-0014`, all now `interactions/verified/`). `DelneyEsperSentinelDoubleTest`: playerB casts Divination with exactly enough mana (none spare for Esper Sentinel's {1} tax), forcing both independent pay-or-decline instances to resolve as "decline" — playerA draws 2. `DelneyRunicArmasaurDoubleTest`: playerB activates Mishra's Factory's non-mana land ability, both independent "may draw" instances accepted — playerA draws 2, passed on the first attempt. `DelneySpellseekerDoubleTest`: casting Spellseeker (not fixture-placed, so its ETB genuinely fires) with two distinct cheap instants in the library found BOTH of them (Opt and Shock) via the two independently-targeted search instances — passed on the first attempt, and the strongest available engine evidence that Delney's doubled instances are not collapsible into one. `DelneyArchivistOfOghmaDoubleTest`: playerB activates Evolving Wilds (an opponent library search), both mandatory instances resolve — playerA gains 2 life and draws 2, passed on the first attempt. Rules citations for all four: CR 603.2d, CR 603.3b. Backing gold board states `GBS-0005.json`..`GBS-0008.json`. Also added a permanent modeling-requirement note to `docs/ARCHITECTURE.md` Layer 2 (prompted directly by user review of `INT-0007`): future Layer 5 encoding of any trigger-doubling/copy-effect interaction must preserve each instance's independent targets, optional choices, modes, and ordering, not collapse them into a single atomic aggregate effect — cross-referenced from each affected interaction record's `prerequisites.other`. | 2026-08-12 |
| SIM-0013 | INTERACT | `INT-0011` (Derevi, Empyrial Tactician + Gaea's Cradle) and `INT-0012` (Clever Impersonator + Birthing Pod) — the last 2 of the 7 "cheap" rules-only candidates — needed Level 4 validation. | `INT-0012`: engine-testing the exact "cast Clever Impersonator, choose to copy" sequence proved unreliable via this project's usual setChoice/addTarget cost-payment approach (a real methodology finding — `activateAbility()`'s cost-payment choices route through an internal AI fallback for this call shape, undocumented outside a source comment in `CardTestPlayerAPIImpl`). Worked around by testing the actual disputed claim directly: two literal Birthing Pod permanents, both independently activated in one turn (`CleverImpersonatorBirthingPodTest`, 1/1 passing) — Clever Impersonator's copy mechanism itself (CR 707) is separately well-established and wasn't the open question. `INT-0011`: engine-verified the single-attacker base case (Derevi's combat-damage trigger untaps a previously-tapped Gaea's Cradle, which is then tapped again for mana within the same combat damage step — `DereviGaeasCradleTest`, 1/1 passing after fixing a choice-registration order bug). A two-attacker extension to verify the original "scales with attacker count" claim did not complete successfully within the effort budget — logged as new open item `SIM-0012` rather than silently asserted or dropped; `INT-0011`'s result summary is scoped to only claim what was actually verified. `INT-0011` and `INT-0012` promoted to `interactions/verified/`; backing gold board states `GBS-0009.json`, `GBS-0010.json`. This closes `SIM-0007`'s entire "cheap, rules-only" sub-population — the 5 remaining candidates are exactly the mana-accounting-sensitive Tithe/Oboro group. | 2026-08-12 |
| SIM-0014 | INTERACT | User audit of commit `51bd25f` (`SIM-0013`) found two problems with `INT-0012`: (1) a factual rules-text error claiming Clever Impersonator's own text says it "becomes an artifact in addition to its other types" - it does not; the Artifact type comes entirely from copying Birthing Pod's own copiable Artifact type per CR 707.2, not from any type-adding clause on Clever Impersonator itself; (2) a validation-standard issue - the executable-engine test (`CleverImpersonatorBirthingPodTest`) reproduces only a downstream consequence (two literal Birthing Pod permanents, each independently activatable) rather than Clever Impersonator's own copy transition, but the record had been marked `status: verified` without distinguishing that from an exact reproduction, conflating two different evidentiary strengths. | Fixed the rules-text error in `interactions/verified/INT-0012.json`. Formalized a 4-tier verification taxonomy (`docs/VERIFICATION_LEVELS.md`): `RULES_VERIFIED`, `ENGINE_COMPONENT_VERIFIED`, `ENGINE_EXACT_VERIFIED`, `CONDITIONAL` - added as a required `verification_level` field (plus `verification.executable_engine_check.exact_reproduction: boolean`) to `data/schemas/interaction.schema.json`, with an `if/then` requiring it whenever `status: verified`. Reclassified `INT-0012` as `ENGINE_COMPONENT_VERIFIED` (was ambiguously just `status: verified`); reclassified the other 8 verified interactions as `ENGINE_EXACT_VERIFIED` after confirming each genuinely reproduces its own exact stated transition end-to-end (none of the other 8 had this problem - only `INT-0012`, since it was the only one where the engine evidence and the interaction's own named transition diverged). Also refined `INT-0011`'s CR citation for the multi-attacker priority mechanism per a secondary review note: cited CR 117.3b specifically (the active player receives priority after an ability resolves) rather than the less precise "CR 603.3b, resolving one at a time with priority in between," since 603.3b only governs the order abilities go ON the stack, not the priority window between resolutions. Added a permanent regression test, `rules_tests/regression/test_verification_levels.py`, checking every verified interaction has a valid `verification_level` and that it's logically consistent with `exact_reproduction` (the exact inconsistency this incident produced). | 2026-08-12 |
| SIM-0007 | INTERACT | 5 of 14 candidate interactions (`INT-0001`, `INT-0003..0006`) remained unverified against Comprehensive Rules text / an executable engine. | Resolved all 5 via exact primary-Oracle-text mana accounting (not Spellbook's paraphrase): `INT-0003` (Hazel's Brewmaster + Devoted Druid) is fully deck-controlled infinite green mana, `RULES_VERIFIED`/`conditional:false`. `INT-0001`/`0004`/`0005` (Faerie Mastermind + Smothering Tithe variants) are `RULES_VERIFIED`/`conditional:true` (opponents must decline Smothering Tithe's tax) - re-derivation found `INT-0001` is only mana-neutral at Spellbook's stated 2-opponent minimum (needs 3+ to accumulate), while `INT-0004`/`0005` are genuinely net-positive at their stated 3-opponent threshold. `INT-0006` (Oboro Breezecaller + Talon Gates + Gaea's Cradle) is `RULES_VERIFIED`/`conditional:false`, corrected to mana-neutral (not "infinite mana") at Spellbook's stated 5-creature threshold - net = creatures-5, so infinite landfall/phase-out repetition holds at 5 creatures but accumulating mana needs 6+. All 5 promoted to `interactions/verified/`. Zero candidates remain. | 2026-08-12 |
| SIM-0015 | INTERACT | Final bounded interaction-discovery sweep across Survival of the Fittest, Deathrite Shaman, Minamo, Shifting Woodland, Enduring Vitality, Badgermole Cub, and remaining Delney/copy-target space. | One new material finding: `INT-0015` (Delney doubles Badgermole Cub's triggered mana bonus, +2 green flat per creature-mana-tap), `RULES_VERIFIED`/`conditional:false`. Minamo+Gaea's Cradle (bounded, once-per-turn, not infinite) and Enduring Vitality (grants creatures a mana ability - a deckbuilding synergy, not a discrete new interaction) reviewed and deliberately deferred as low-impact. Survival of the Fittest and Deathrite Shaman confirmed to have zero Spellbook-indexed combos and no material new interaction found. | 2026-08-12 |
