6 candidate interactions (`INT-0001.json` .. `INT-0006.json`), pulled from
Commander Spellbook for the SIM-001 subject deck via `sim/ingestion/spellbook.py`
— see `docs/assignments/SIM-001.md` "Phase 2 — interaction discovery" for the
full write-up of each. None are `status: verified` yet; Phase 3 exact-line
validation is tracked as `coverage_backlog` `SIM-0007`. The independent
pairwise/triple discovery scan (not limited to Spellbook's index) per the
charter's Phase 2 requirements is still to do.
