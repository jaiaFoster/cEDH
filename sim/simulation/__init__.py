"""Layer 5 - Simulation: the four-player game loop.

Not implemented yet - and per the charter's initialization behavior, must
not be implemented before Validation Gates 1-3 exist to check it against
(docs/VALIDATION_GATES.md). Planned responsibility: seats, hidden/public
information, stack interaction, win attempts, responses, resource
expenditure, consuming Layers 1-4. Every run's config must conform to
data/schemas/simulation_result.schema.json and be recorded per
docs/VERSIONING.md before its output is treated as evidence.

Blocked on: Gates 1-3, which are blocked on the subject decklist and data
access (see coverage_backlog/BACKLOG.md).
"""
