# Results

- `raw/<run_id>/` — full provenance-tagged simulation output, immutable once
  written (`docs/VERSIONING.md`). Config conforms to
  `data/schemas/simulation_result.schema.json`.
- `findings_packets/<slug>.md` — primer-facing distillations in the
  charter's Findings Packet format (Finding, Evidence, Interpretation,
  Pilot/Deckbuilding Implication, Provenance, Confidence/Limitation).

Both empty — no runs have happened yet; per `docs/VALIDATION_GATES.md`,
Gate 1 hasn't started.
