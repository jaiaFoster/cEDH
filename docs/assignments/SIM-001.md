# SIM-001 — Comprehensive Baseline Simulation & Metagame Census

**Status:** Phase 0 complete. Phase 2 (interaction discovery) started and
producing real results. Phase 1 (subject-deck rules model proper) and
Forge/XMage adapter selection (`INFRA-0003`) are the next concrete build
tasks.
**Priority:** Critical (per assignment).
**Subject deck version:** `tymna-thrasios-treefarm-v1` (real, schema-conformant — `data/decklists/tymna-thrasios-treefarm-v1.json`)
**Assigned:** 2026-08-12

This document tracks execution of SIM-001 against its own 35-phase plan and
execution order. It is the live status board for this assignment — update it
as phases complete rather than letting status live only in chat history.

## Network status: OPEN (as of 2026-08-12, mid-assignment)

This environment's outbound egress was blocked to every SIM-001 data source
at kickoff (`ENV-0001`). The user updated the environment's network policy
mid-session; re-tested and confirmed live with real content pulls from
Scryfall, MTGJSON, Commander Spellbook, TopDeck.gg, EDHTop16, GitHub, and
raw.githubusercontent.com (Moxfield remains separately blocked by its own
Cloudflare bot-wall, not by this environment — tracked as part of
`SIM-0001`). See `docs/SOURCES.md` and `docs/EGRESS_ALLOWLIST.md`.
`ENV-0001`, `INFRA-0001`, and `INFRA-0002` are resolved in
`coverage_backlog/BACKLOG.md`.

**Practical effect:** Phases 1-2 and 6-11, previously stated as blocked, are
now actually workable, and Phase 2 has real output already (see below). This
document reflects that — phases below are marked "blocked" only where a
real, specific blocker remains, not as a blanket network excuse.

## Phase status

| Phase | Description | Status | Notes |
|---|---|---|---|
| 0 | Verify the deck | **Complete** | See below. 100/100 cards (98 + 2 commanders) bulk-ingested from Scryfall with full Oracle text, mana cost, color identity, rulings. Zero color-identity violations (programmatically verified). |
| 1 | Subject-deck rules model | **In progress** | Card-level data exists (`data/cards_cache/oracle-2026-08-12/`) with heuristic ability classification (`sim/ingestion/scryfall.py`). Both Forge and XMage confirmed 100/100 card coverage (`INFRA-0002`, resolved) — actual `sim/rules_engine/` adapter build against one or both is the next concrete task, tracked as `INFRA-0003`. |
| 2 | Interaction discovery | **In progress, real output** | Built `sim/ingestion/spellbook.py`; queried Commander Spellbook's `/variants` endpoint once per subject-deck card (3,853 unique variants touched). Found 6 combos fully playable with cards already in the deck (`interactions/candidate/INT-0001..0006.json`) and 112 one-card-away near-misses (`interactions/one_card_away_from_deck.json`). Directly confirms 3 of the assignment's named seeds — see below. Independent pairwise/triple discovery (not just what Spellbook indexes) still to do. |
| 3 | Exact-line validation | Not started | Needs Phase 2 output + Forge/XMage (`INFRA-0002`) or Comprehensive Rules citation work. |
| 4 | Combo dependency graph | Not started | Needs Phase 3. |
| 5 | Subject-deck gold tests | Not started | Needs Phase 1-4; can start drafting against real card data now. |
| 6 | Metagame census | Not started | TopDeck.gg and EDHTop16 (`edhtop16.com/api/graphql`) both confirmed live. Ready to start once Phase 1-5 (subject-deck fidelity) are in reasonable shape, per charter execution order — census can run in parallel but shouldn't outpace subject-deck modeling by too much. |
| 7 | Long tail inclusion | Not started | Depends on Phase 6. |
| 8 | Archetype clustering | Not started | Depends on Phase 6-7. |
| 9 | Representative deck selection | Not started | Depends on Phase 6-8. |
| 10 | Archetype policy modeling | Not started | Depends on Phase 8-9. |
| 11 | Real-pod distribution | Not started | Depends on Phase 6. |
| 12 | Validation gates A-F | Not started | Gate A needs Phase 0-5; see `docs/VALIDATION_GATES.md`. |
| 13-35 | Baseline simulation through Findings Packet | Not started | Blocked on Phase 12 (all gates) passing, per charter — no production simulation before validation. |

## Phase 0 — final findings

### 0.1 / 0.3 — Card count and legality

- Main deck: 98 unique cards, quantity 1 each, no duplicates.
- Commanders: Tymna the Weaver (color identity W/B), Thrasios, Triton Hero
  (color identity G/U). Combined: **W U B G**.
- **Zero color-identity violations**, confirmed two ways: (1) a full
  Scryfall bulk pull of all 100 cards, cross-referenced programmatically
  against the WUBG commander identity — every card's color identity is a
  subset; (2) manual spot-checks during the pre-bulk-pull WebSearch phase
  (see below). This is now backed by authoritative structured data, not
  search snippets or memory.
- **Badgermole Cub**, banned in **Standard** on 2026-08-10 (2 days before
  this assignment) — confirmed via bulk pull's `legalities` field: Standard
  `not_legal`... `commander: legal`. **Legal for this deck.**
- **Moxfield source still not independently re-pulled.** `moxfield.com` and
  `api.moxfield.com` are both reachable at the network level now, but
  Moxfield's own Cloudflare bot-wall returns 403 ("Please enable cookies")
  to both curl and WebFetch. The 98-card list is still a transcription from
  the assignment text, not a live re-pull. Tracked as `SIM-0001`, risk
  narrowed (see below) but not closed.

### 0.2 — Unfamiliar/ambiguous cards, and a real ingestion pipeline

Built `sim/ingestion/scryfall.py` — a real (not stub) adapter that bulk-pulls
via Scryfall's `/cards/collection` endpoint (2 requests for 100 cards) plus
per-card rulings (100 requests, rate-limited), and writes
`card.schema.json`-conformant records. All 100/100 names resolved on the
first pull (the one apparent miss, King T'Challa, was just returned under
its full transform-card name `King T'Challa // Black Panther, Hope
Enduring` — expected Scryfall behavior for DFCs, not an error).

Headline findings, now from authoritative data rather than search snippets:

- **La abundancia de Yucahú is Sylvan Library** (Spanish/Portuguese Secret
  Lair alt-name printing, Oracle-identical). Ingested under its canonical
  name.
- **Talon Gates of Madara is a plain Modern Horizons 3 Commander land**, not
  Naruto-themed despite the name.
- **Commandeer's exact cost confirmed: `{5}{U}{U}`**, alt cost confirmed
  (exile two blue cards). Resolves `SIM-0003`.
- **Volatile Stormdrake is the only Energy ({E})-using card in the 98**,
  confirmed via full ingestion, not just spot-check. `SIM-0004` stays open —
  this is a rules-engine design requirement now, not a data gap.
- **King T'Challa and Shang-Chi confirmed Commander-legal** via the bulk
  pull's own `legalities.commander: "legal"` field — no longer just a
  search-snippet-level claim.

**Important methodology finding — a WebSearch "verification" turned out to
be wrong.** During the pre-bulk-pull spot-check pass, WebSearch reported
Swift Reconfiguration as mono-blue `{U}` from "Kamigawa: Neon Dynasty
Commander." The real Scryfall data says mono-white `{W}`, with rulings dated
2022-02-18 matching the original Kamigawa: Neon Dynasty printing. Neither
version breaks WUBG legality (W is in-identity either way), but this is a
concrete, in-repo case of exactly what the charter's source hierarchy
predicts: a Tier-6-adjacent source (WebSearch synthesis) contradicted by
Tier-3 authoritative structured data (Scryfall), and the authoritative
source was right. Four more `well_known` cards also had wrong details
surface only once the bulk pull ran: Avacyn's Pilgrim, Delighted Halfling,
Elves of Deep Shadow, and Gaea's Cradle were all under-scoped on color
identity — each has a **mana ability that adds a color not reflected in the
card's casting cost**, which a name-recognition-only check missed but which
color identity rules (CR 903.4) require counting. All logged as `SIM-0006`,
resolved, no legality impact. **Takeaway for the rest of this project:**
treat any "verified_by_search" or "well_known" tag from before 2026-08-12's
network fix as provisional, superseded now by the real card cache.

### What Phase 0 has NOT done

- Independent re-confirmation of the Moxfield source list against a live
  pull (`SIM-0001`, open — blocked by Moxfield's bot-wall, not this
  environment).
- MTGJSON cross-validation of the 100 ingested cards (planned next step, not
  yet run).
- Manual review of the heuristic ability classifier's output
  (`sim/ingestion/scryfall.py`'s `kind` field per ability) — it's a
  reasonable first pass (verified correct on spot-checks: Gaea's Cradle,
  Devoted Druid, Commandeer all classified sensibly) but not a substitute
  for Level 4 exact-line review before any interaction is marked verified.

## Phase 2 — interaction discovery, first pass (real findings)

Ran `sim/ingestion/spellbook.py` against all 100 subject-deck card names via
Commander Spellbook's public `/variants?q=card="..."` search (the
`find-my-combos` endpoint requires auth we don't have). Touched 3,853 unique
variants; classified each by whether every card it uses is already in the
deck. Six are:

1. **Devoted Druid + Swift Reconfiguration → infinite green mana.** Swift
   Reconfiguration turns Devoted Druid into a Vehicle (not a creature unless
   crewed), so the -1/-1 counters from its untap ability never trigger a
   "0 toughness" death — tap for G, add a counter to untap, repeat.
2. **Devoted Druid + Hazel's Brewmaster → infinite green mana (a second,
   independent line).** Hazel's Brewmaster exiles a graveyard creature card
   and makes a Food that gains all its activated abilities; a Food isn't a
   creature, so a Food that "is" Devoted Druid via this effect has the same
   "-1/-1 counters don't kill it" property. Needs Devoted Druid in the
   graveyard first.
3. **Faerie Mastermind + Smothering Tithe + (Clever Impersonator | Kinnan,
   Bonder Prodigy | Training Grounds) → infinite card draw / Treasures**,
   three separate variants. This directly confirms the assignment's named
   seed "Smothering Tithe / Faerie Mastermind loops."
4. **Oboro Breezecaller + Talon Gates of Madara + Gaea's Cradle → infinite
   green mana + infinite landfall triggers + repeatable phase-out.** Loop:
   tap Cradle, tap Talon Gates for {C}, pay {2} to bounce Talon Gates via
   Oboro Breezecaller (untapping Cradle), replay Talon Gates for {4} from
   hand (retriggering its phase-out ETB). Spellbook states "at least five
   creatures" as the prerequisite; **this agent's own mana-accounting pass
   suggests the loop may only break even at exactly five creatures and need
   six+ to be truly infinite** — flagged as `SIM-0007`, needs precise Phase
   3 verification, not taken on faith.

Also logged 112 "one card away" near-misses to
`interactions/one_card_away_from_deck.json` (not written as candidate
interactions — they aren't real lines in this exact 98). Most notable:
Hullbreaker Horror appears as the single missing piece across three
separate high-popularity Sol Ring / Mana Vault / Mox Amber storm-count
loops — a genuine deckbuilding data point for the Phase 33 ablation
framework later, not acted on now.

**All 6 are `status: candidate`, not `verified`.** Per charter non-negotiable
rule 1, none may be used as a deterministic transition in simulation until
Phase 3 (exact-line validation, ideally cross-checked against an executable
engine) confirms them.

## Forge/XMage evaluation (`INFRA-0002`, resolved)

Shallow-cloned both engines to scratch space (not committed to this repo):
`Card-Forge/forge` (815MB, `--depth 1 --filter=blob:none`) and
`magefree/mage` (164MB, sparse-checkout of the card-script directory only).
Checked all 100 subject-deck cards by name against each engine's card
files: **100/100 in Forge, 100/100 in XMage.** Spot-checked Forge's scripts
for Devoted Druid, Swift Reconfiguration, and Volatile Stormdrake against
the Scryfall-pulled Oracle text — all three match exactly, and Volatile
Stormdrake's Energy mechanic is natively implemented (`CounterType$ ENERGY`).
Both engines are viable candidates for Level 4 execution; the remaining
question (ergonomics of driving either programmatically, and Level 3
many-games-per-second performance) is tracked as new `INFRA-0003`, not
urgent enough to block Gate 2 gold-state work.

## Coverage backlog state after this session

Resolved: `ENV-0001`, `INFRA-0001`, `INFRA-0002`, `DECK-0001`, `SIM-0002`,
`SIM-0003`, `SIM-0005`, `SIM-0006`.
Open: `SIM-0001` (Moxfield re-pull), `SIM-0004` (Energy mechanic design for
a native tracker), `SIM-0007` (Phase 3 validation owed to the 6 candidate
interactions, including the INT-0006 creature-count question), `INFRA-0003`
(Forge vs. XMage adapter-build decision). See `coverage_backlog/BACKLOG.md`
for full entries.

## Immediate next steps

1. Decide Forge vs. XMage vs. both for the actual `sim/rules_engine/`
   adapter (`INFRA-0003`) — likely needs a small spike: drive each engine
   through one predefined board state and compare ergonomics/speed before
   committing.
2. Phase 3: validate the 6 candidate interactions found above against
   Comprehensive Rules text and, ideally, reproduce them in whichever engine
   is chosen — resolves `SIM-0007`, most urgently the INT-0006 creature-count
   question.
3. Run an independent pairwise/triple discovery scan per the charter's
   Phase 2 requirements (not limited to what Spellbook's index surfaces) —
   Commander Spellbook is a seed, not the complete graph, per charter.
4. Cross-validate the 100 cards against MTGJSON (now reachable, not yet
   done).
5. In parallel: pull TopDeck.gg + EDHTop16 (`edhtop16.com/api/graphql`) data
   for the ~6-month and ~12-month windows ending 2026-08-12, to start Phase
   6 (metagame census) — can run alongside 1-4 since it's a different data
   source and doesn't block subject-deck work.
